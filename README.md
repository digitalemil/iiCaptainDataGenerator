iiCaptainDataGenerator
======================

As this is a very simple thing there's is no pom.xml or build.xml. 
Just compile with:
cd src
javac -cp "../lib/*" -d ../bin de/digitalemil/iicaptain/dg/*
javac -d ../bin de/digitalemil/iicaptain/appannie/*
cd ..
